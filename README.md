### 1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
### Ans: 
### 	getElementById give us for selected single unic element by element id.
###	  	getElementsByClassName give us for selected single or multiple elements which elements have same type of class.
###	  	And querySelector/querySelectorAll is css selector , it working like css selector.
###	  	on the other perpose querySelectorAll give extra nodeList.

### 2. How do you create and insert a new element into the DOM?
### Ans: i using 3 step for create and insert element into Dom .
### step-1: select the Dom where i can insert element.
### step-2: create an element in a variable using createElement(like "div").
### step-3: insert the created element intu selected dom using appendChild(variable name)

### 3. What is Event Bubbling? And how does it work?
### Ans: i know event bubbling is a intresting system for elemect event operation it delegate dom and working on dom depend by parent and childlist, it working on parent dependent dom.
### 4. What is Event Delegation in JavaScript? Why is it useful?
### Ans: event delegation is a dom event design pattern for event delegate, it is very usefull system, we manange multiple state event element depended on there parent, so it is very usefull system.


### 5. What is the difference between preventDefault() and stopPropagation() methods?
### Ans: preventDefault() using for remove default behaviour and stopPropagation() stop the event delegation.


